import React from 'react';
import { Ionicons } from '@expo/vector-icons';

export default class Icon extends React.Component {
    render() {
        return (
            <Ionicons name="ios-add-circle" size={80} color="#4e7373" />
        );
    }
}